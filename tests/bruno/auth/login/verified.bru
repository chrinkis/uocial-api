meta {
  name: verified
  type: http
  seq: 1
}

post {
  url: {{api}}/auth/token/generate
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  email: verified@uoc.gr
  password: password
}

body:multipart-form {
  : @file()
}

script:post-response {
  bru.setVar("token", res.body.token.plainTextToken);
}

settings {
  encodeUrl: true
}
